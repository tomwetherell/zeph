[package]
name = "zeph"
version = "0.1.0"
edition = "2021"
description = "A CLI tool for inspecting zarr data stores"

[dependencies]
crossterm = "0.28"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
